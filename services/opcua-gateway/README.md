# Service: opcua-gateway

OPC-UA client/server (Eclipse Milo): subscribe tag RO, write tag R/W, heartbeat, backfilling, redundancy. Map tag thô -> event canonical. Tham chiếu rules/20 + docs/architecture/opc-ua-tag-contract.md.

Trạng thái: skeleton (chưa hiện thực). Tạo module bằng skill `scaffold-service`; bỏ comment trong services/pom.xml khi bắt đầu. Xem docs/PLAN.md để biết thuộc phase nào.

